# 全体系公共默认填充

> 项目内单源：9 个领域 skill 共用。各 SKILL.md 的默认块引用本文件，只保留本线特有的尺度/着装/场景默认。
> 规则：**仅填空**——用户已锁定的值不改；空位才用默认，并标「建议」。

## 1. 身份

| 维 | 默认 |
|----|------|
| 人种 | East Asian 成年女（男主 adult East Asian man） |
| 年龄 | early twenties（职业/老年题材随用户） |
| 人数 | solo（用户点双人/群像再写） |

## 2. 成片

| 维 | 默认 |
|----|------|
| 时长 | 5 秒 / 124 帧（17k+5 对齐） |
| 画幅 | 竖 9:16（静帧与视频相同，方便 R2VA）；封面需求 3:4；SFW 街拍/纪实可 16:9 随题材 |
| 镜头 | locked-off 或一次极慢 push-in；5 秒一镜一个主动作 |
| 结构 | 一套 N 条 = N 条独立成片，每条有独立起止；不要一条 5 秒切多场 |

## 3. 声画

| 维 | 默认 |
|----|------|
| 对白 | 无（用户给台词才写原文） |
| 配乐 | N/A |
| 拟音 | 只写该场必然拟音（环境底、衣料、脚步、接触）；露骨线性行为的女方呻吟/吸吮/拍打属于必然拟音（见 creative-director sound-library），其余呻吟/叹气事件不编 |

## 4. 工程

| 维 | 默认 |
|----|------|
| 工作流 | 本机用户库「导演出图」「导演出视频」；不保存 ComfyUI 用户库工作流 |
| 校验 | 视频提示词落盘前跑 `../minimax-h3-core/scripts/validate_prompt.py`，ERROR 清零才准提交 |

## 5. 线内特有空位

以下只写去哪查，不在这里另建目录：

| 线 | 特有空位 | 事实源 |
|----|---------|--------|
| 露骨 | E 级 / 暴露 / 体液结局 / 幻想 | `../erotic-prompt/references/exposure-scale-defaults.md` |
| 纯欲 | I 级 / 暴露上限 | 对应静帧 skill 主文 |
| SFW | 大区 A–I / 着装 | 对应静帧 skill 主文 |

各线 SKILL.md 的「默认填充」表只保留这些特有行；人种/年龄/时长/画幅等公共行不重复抄写。
